# AI 기능 풀백 처리 개선

## 문제

1. **크레딧 부족/API 에러** 시 사용자에게 `"스타일 추천에 실패했습니다"` 같은 모호한 메시지만 표시
2. **이미지 생성 전체 실패** 시에도 GPT-4o **텍스트 분석 결과는 이미 완료**되었지만, 이미지 에러로 전체가 실패 처리될 수 있음

## 해결 방안

### 1. 크레딧/API 에러 감지 + 사용자 친화적 메시지

![](Pasted%20image%2020260218112247.png)